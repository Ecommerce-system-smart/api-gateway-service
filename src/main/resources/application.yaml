server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          # Bật khi ở Local (dùng Eureka), tắt khi lên K8s (dùng DNS)
          enabled: ${GATEWAY_DISCOVERY_ENABLED:true}
      routes:
        - id: product-service
          # Local dùng lb:// (Eureka), K8s dùng http://product-service:8081
          uri: ${PRODUCT_SERVICE_URI:lb://product-service}
          predicates:
            - Path=/api/products/**

        - id: order-service
          # Local dùng lb:// (Eureka), K8s dùng http://order-service:8082
          uri: ${ORDER_SERVICE_URI:lb://order-service}
          predicates:
            - Path=/api/orders/**

        - id: notification-service
          uri: ${NOTIFICATION_SERVICE_URI:lb://notification-service}
          predicates:
            - Path=/api/notifications/**

  security:
    oauth2:
      resourceserver:
        jwt:
          # URL của Keycloak. Ở bài trước realm của bạn là ecommerce-service,
          # nếu bạn đổi thành ecommerce-realm thì hãy giữ nguyên.
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:31000/realms/ecommerce-realm}

# Cấu hình Eureka
eureka:
  client:
    # Cho phép K8s tắt Eureka đi để không bị báo lỗi "Connection Refused 8761"
    enabled: ${EUREKA_CLIENT_ENABLED:true}
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

# --- CÁC CẤU HÌNH DƯỚI ĐÂY GIỮ NGUYÊN VÌ LÀ LOGIC ---
resilience4j:
  circuitbreaker:
    instances:
      productService:
        failure-rate-threshold: 50
        wait-duration-in-open-state: 5000
        sliding-window-size: 5
  timelimiter:
    instances:
      productService:
        timeout-duration: 3s

logging:
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      # URL của Zipkin, cho phép ghi đè khi đưa lên K8s
      endpoint: ${ZIPKIN_URL:http://localhost:9411/api/v2/spans}
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
    metrics:
      tags:
        application: ${spring.application.name}